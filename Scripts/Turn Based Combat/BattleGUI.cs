using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleGUI : MonoBehaviour {

	// Use this for initialization
	void OnGUI()
    {
        if (GUI.Button(new Rect(400f,225f, 75f, 25f), "Attack"))
        {

        }
    }
}
